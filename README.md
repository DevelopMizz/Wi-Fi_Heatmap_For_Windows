# Wi-Fi_Heatmap_For_Windows
This is a a Wi-Fi heatmapping tool build on .Net with native support for Windows

## Dependencies  
.Net 9 Core Runtime

## Build instructions 
from within the main project folder open a terminal and run.
```Powershell
dotnet build --configuration Release
```
